micronaut:
   environment: test
datasources:
   default:
      url: "jdbc:postgresql://localhost:7432/cynergidbtest"
      username: postgres
      password: password
      maximumPoolSize: 1
flyway:
   datasources:
      default:
         clean-on-validation-error: true
         clean-disabled: false
